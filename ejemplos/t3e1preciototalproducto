#Pedimos datos
producto = input("Nombre del producto: ")
precio = float(input("Precio por unidad: "))
cantidad = int(input("Cuanto va a comprar: "))
descuento = float(input("Que descuento tiene (%): "))
iva = float(input("Que porcentaje de IVA (%) tiene: "))

#Creamos las funciones
def calcular_total_producto (unidad, cantidad):
    return precio_total
def calcular_descuento (total_producto, descuento):
    total_descuento = total_producto - (total_producto * descuento)/100
    return total_descuento

def calcular_IVA (total,iva):
    total_iva = total * iva
    return total_iva

def calcularTotal():
    #solicitud de parametros al usuario
    producto = input("Nombre del producto: ")
    precio = float(input("Precio por unidad: "))
    cantidad = int(input("Cuanto va a comprar: "))
    descuento = float(input("Que descuento tiene (%): "))
    iva = float(input("Que porcentaje de IVA (%) tiene: "))

    total_compra = calcular_total_producto(unidad, cantidad)
    total_descuento = calcular_descuento(total_compra, descuento)
    total_iva = calcular_IVA(total_descuento, iva)


#Muestra resultado final
print("el total de la compra es:" + nombre_del_producto + "es", total_iva, "euros")


calcular_operaciones()